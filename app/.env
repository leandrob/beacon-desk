# Environment variables for the dev stack (auto-loaded by Docker Compose).

# Backend URL the frontend dev server proxies /api and /ws to.
# Inside Docker this is the backend service on the compose network.
# For host-only dev (no Docker) use http://localhost:4100.
BACKEND_URL=http://backend:4100
